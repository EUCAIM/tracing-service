apiVersion: v1
kind: Service
metadata:
  name: immudb
  namespace: tracing-service
spec:
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: immudb-psql
  selector:
    app: immudb